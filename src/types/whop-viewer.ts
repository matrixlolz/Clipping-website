/** Whop member profile for the iframe viewer (see `getWhopViewerForExperience`). */
export type WhopViewer = {
  id: string;
  name: string | null;
  username: string;
  profilePictureUrl: string | null;
};
